#ifndef _CAMERA_H_
#define _CAMERA_H_

#include "utils.h"

struct Camera
{
public:
	int x, y;
	int width, height;

	Camera(int x, int y, int w, int h)
	{
		this->x = x;
		this->y = y;
		width = w;
		height = h;
	}
	
	Camera(){}

	void apply();

private:

};

#endif